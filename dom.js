// no hay espacios entre el document, el punto y el metodo
// getElementById -> es traer un elemento del HMTL por su ID

// SELECCIONAR ELEMENTOS DEL DOM
/*let nameInput = document.getElementById('nombre')
console.log(nameInput);

// query selector me trae cualquier tipo de selector -> etiqueta, id, clase
let saludoP = document.querySelector('.mostrarSaludo');
console.log(saludoP);
*/



// FUNCIONES .> SON BLOQUES QUE ENCAPSULAN CÓDIGO, Y EJECUTAN ACCIONES

/*function saludarUsuario() {
    // aca va el codigo que quiero ejecutar
    // acceder al contenido de nuestras etiquetas
    let name = nameInput.value;
    console.log(name)

    let mensaje = '';
    if(name){
        mensaje = 'Hola' + name;
        // textoContent me permite cambiar el texto dentro de etiquetas
        saludoP.textContent = mensaje;
}else{
    // si no se ingreso nombre -> else = sino
    mensaje = 'Hola Desconocido';
    saludoP.textContent = mensaje;
}
}
*/



